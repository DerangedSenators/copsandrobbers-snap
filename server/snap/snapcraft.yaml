name: copsandrobbers-server # you probably want to 'snapcraft register <name>'
base: core18 # the base snap is the execution environment for this snap
version: '3.0' # just for humans, typically '1.2+git' or '1.3.2'
summary: Server Application for Cops And Robbers # 79 char long summary
description: |
	This is the server application for the DerangedSenators CopsAndRobbers Game developed in Unity

grade: devel # must be 'stable' to release into candidate/stable channels
confinement: devmode # use 'strict' once you have the right plugs and slots

parts:
  my-part:
    # See 'snapcraft plugins'
    plugin: nil
